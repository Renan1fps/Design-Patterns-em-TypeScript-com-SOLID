//a responsabilidade desse reporsitório é adiconar o cliente no repositório mas ele não deve conhecer o banco de dados
class ClienteRepository {
  AdicionarCliente(Cliente) {
    const conectionDb = [];
    conectionDb.push(Cliente);
  }
}

module.exports =ClienteRepository;
